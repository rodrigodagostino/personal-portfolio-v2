---
export interface Props {
  id: string
  title?: string
}
const { id, title } = Astro.props
---

<section id={id}>
  <slot name="header">
    {title && <h2>{title}</h2>}
  </slot>
  <slot />
</section>

<style>
  section {
    min-height: 100vh;
  }
</style>
